name: Build Onsamiro Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Extract source ZIP
        shell: bash
        run: |
          set -e

          ZIP="$GITHUB_WORKSPACE/onsamiro_source_v3111_no_thermal_stop.zip"

          if [ ! -f "$ZIP" ]; then
            echo "Source ZIP not found: $ZIP"
            exit 1
          fi

          mkdir -p "$GITHUB_WORKSPACE/extracted"
          unzip -q "$ZIP" -d "$GITHUB_WORKSPACE/extracted"

          echo "Source ZIP extracted successfully"

      - name: Find Android project
        shell: bash
        run: |
          set -e

          PROJECT_DIR="$(find "$GITHUB_WORKSPACE/extracted" \
            -type f \
            -name settings.gradle \
            -printf '%h\n' \
            | head -n 1)"

          if [ -z "$PROJECT_DIR" ]; then
            echo "Android project not found"
            find "$GITHUB_WORKSPACE/extracted" -maxdepth 3 -type f | head -100
            exit 1
          fi

          echo "PROJECT_DIR=$PROJECT_DIR" >> "$GITHUB_ENV"
          echo "Android project found:"
          echo "$PROJECT_DIR"

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android toolchain
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true

          sdkmanager \
            "platform-tools" \
            "platforms;android-35" \
            "build-tools;35.0.0" \
            "ndk;28.0.13004108" \
            "cmake;3.22.1"

      - name: Fetch whisper.cpp
        shell: bash
        run: |
          cd "$PROJECT_DIR"
          bash ./scripts/fetch_whisper.sh

      - name: Run static checks
        shell: bash
        run: |
          cd "$PROJECT_DIR"

          bash ./scripts/static_audit.sh

          python3 ./tools/NoThermalStopSelfTest.py
          python3 ./tools/ThermalRecoverySelfTest.py

          mkdir -p build/qgtest

          javac \
            -d build/qgtest \
            app/src/main/java/com/onsamiro/transcriber/QualityGuard.java \
            tools/QualityGuardSelfTest.java

          java -cp build/qgtest QualityGuardSelfTest

      - name: Build debug APK
        shell: bash
        run: |
          cd "$PROJECT_DIR"

          gradle \
            --no-daemon \
            --stacktrace \
            :app:assembleDebug

      - name: Verify APK
        shell: bash
        run: |
          cd "$PROJECT_DIR"

          APK="app/build/outputs/apk/debug/app-debug.apk"

          if [ ! -f "$APK" ]; then
            echo "APK was not created"
            exit 1
          fi

          bash ./scripts/verify_apk.sh "$APK"

      - name: Prepare final APK
        shell: bash
        run: |
          mkdir -p "$GITHUB_WORKSPACE/dist"

          cp \
            "$PROJECT_DIR/app/build/outputs/apk/debug/app-debug.apk" \
            "$GITHUB_WORKSPACE/dist/온새미로_자동전사_v3.1.11_발열중단제거.apk"

          cd "$GITHUB_WORKSPACE/dist"

          sha256sum \
            온새미로_자동전사_v3.1.11_발열중단제거.apk \
            > 온새미로_자동전사_v3.1.11_발열중단제거.sha256

          ls -lh

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: onsamiro-auto-transcriber-v3.1.11-no-thermal-stop
          path: |
            dist/온새미로_자동전사_v3.1.11_발열중단제거.apk
            dist/온새미로_자동전사_v3.1.11_발열중단제거.sha256
          if-no-files-found: error
          retention-days: 30      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK / NDK / CMake
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true

          sdkmanager \
            "platform-tools" \
            "platforms;android-35" \
            "build-tools;35.0.0" \
            "ndk;28.0.13004108" \
            "cmake;3.22.1"

      - name: Fetch whisper.cpp
        shell: bash
        run: |
          cd "$PROJECT_DIR"
          bash ./scripts/fetch_whisper.sh

      - name: Run static audit
        shell: bash
        run: |
          cd "$PROJECT_DIR"

          bash ./scripts/static_audit.sh
          python3 ./tools/NoThermalStopSelfTest.py
          python3 ./tools/ThermalRecoverySelfTest.py

      - name: Run QualityGuard self test
        shell: bash
        run: |
          cd "$PROJECT_DIR"

          mkdir -p build/qgtest

          javac \
            -d build/qgtest \
            app/src/main/java/com/onsamiro/transcriber/QualityGuard.java \
            tools/QualityGuardSelfTest.java

          java -cp build/qgtest QualityGuardSelfTest

      - name: Build APK
        shell: bash
        run: |
          cd "$PROJECT_DIR"

          gradle \
            --no-daemon \
            --stacktrace \
            :app:assembleDebug

      - name: Verify APK
        shell: bash
        run: |
          cd "$PROJECT_DIR"

          APK="app/build/outputs/apk/debug/app-debug.apk"

          if [ ! -f "$APK" ]; then
            echo "APK was not created"
            exit 1
          fi

          bash ./scripts/verify_apk.sh "$APK"

      - name: Prepare download file
        shell: bash
        run: |
          mkdir -p "$GITHUB_WORKSPACE/dist"

          cp \
            "$PROJECT_DIR/app/build/outputs/apk/debug/app-debug.apk" \
            "$GITHUB_WORKSPACE/dist/onsamiro_auto_transcriber_v3.1.11_no_thermal_stop.apk"

          cd "$GITHUB_WORKSPACE/dist"

          sha256sum \
            onsamiro_auto_transcriber_v3.1.11_no_thermal_stop.apk \
            > onsamiro_auto_transcriber_v3.1.11_no_thermal_stop.sha256

          ls -lh

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: onsamiro-auto-transcriber-v3.1.11
          path: |
            dist/onsamiro_auto_transcriber_v3.1.11_no_thermal_stop.apk
            dist/onsamiro_auto_transcriber_v3.1.11_no_thermal_stop.sha256
          if-no-files-found: error
          retention-days: 30
